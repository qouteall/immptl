import{_ as e}from"./plugin-vue_export-helper.21dcd24c.js";import{o as t,c as a,e as o}from"./app.0c870a62.js";const i={},r=o('<h1 id="portal-attributes" tabindex="-1"><a class="header-anchor" href="#portal-attributes" aria-hidden="true">#</a> Portal Attributes</h1><p>These are the attributes that a portal has.</p><p>You can edit them by <code>/portal set_portal_nbt</code> command. (Don&#39;t use <code>/data</code> command)</p><p><a href="./Portal-Customization">Portal Customization</a></p><h3 id="body-attributes" tabindex="-1"><a class="header-anchor" href="#body-attributes" aria-hidden="true">#</a> Body Attributes</h3><h4 id="orientation-axis" tabindex="-1"><a class="header-anchor" href="#orientation-axis" aria-hidden="true">#</a> Orientation Axis</h4><p>Two unit vectors that are perpendicular to each other. These two vectors define the orientation of the portal.</p><p>NBT tags:<code>axisWX</code> <code>axisWY</code> <code>axisWZ</code> <code>axisHX</code> <code>axisHY</code> <code>axisHZ</code></p><p>It&#39;s recommended to use command <code>/portal rotate_portal_body</code> to edit this attribute.</p><h4 id="width-height" tabindex="-1"><a class="header-anchor" href="#width-height" aria-hidden="true">#</a> Width, Height</h4><p>Width is the portal shape&#39;s length along the direction of <code>axisW</code>. Height is the portal shape&#39;s length along the direction of <code>axisH</code>.</p><p>NBT tags: <code>width</code> <code>height</code></p><p>It&#39;s recommended to use command <code>/portal set_portal_size</code> to edit this attribute.</p><h3 id="transformation-attributes" tabindex="-1"><a class="header-anchor" href="#transformation-attributes" aria-hidden="true">#</a> Transformation Attributes</h3><h4 id="destination-dimension" tabindex="-1"><a class="header-anchor" href="#destination-dimension" aria-hidden="true">#</a> Destination Dimension</h4><p>The dimension id of the portal&#39;s destination dimension.</p><p>NBT tag: <code>dimensionTo</code></p><h4 id="destination" tabindex="-1"><a class="header-anchor" href="#destination" aria-hidden="true">#</a> Destination</h4><p>The destination position.</p><p>NBT tag: <code>destinationX</code> <code>destinationY</code> <code>destinationZ</code></p><h4 id="rotation-transformation" tabindex="-1"><a class="header-anchor" href="#rotation-transformation" aria-hidden="true">#</a> Rotation Transformation</h4><p>Optional. A quaternion that defines the portal&#39;s rotation transformation.</p><p>NBT tag: <code>rotationA</code> <code>rotationB</code> <code>rotationC</code> <code>rotationD</code></p><h4 id="scale-transformation" tabindex="-1"><a class="header-anchor" href="#scale-transformation" aria-hidden="true">#</a> Scale Transformation</h4><p>A number that defines the portal&#39;s scale transformation.</p><p>NBT tag: <code>scale</code></p><h3 id="teleportation-attributes" tabindex="-1"><a class="header-anchor" href="#teleportation-attributes" aria-hidden="true">#</a> Teleportation Attributes</h3><h4 id="whether-teleportation-changes-scale" tabindex="-1"><a class="header-anchor" href="#whether-teleportation-changes-scale" aria-hidden="true">#</a> Whether Teleportation Changes Scale</h4><p>Whether the teleportation changes entity scale if the portal has a scale transformation. It requires Pehkui mod to work.</p><p>NBT tag: <code>teleportChangesScale</code></p><p>Example: Make a portal that has scaling to not change the crossing entity&#39;s scale <code>/portal set_portal_nbt {teleportChangesScale:false}</code></p><h4 id="whether-teleportation-changes-gravity-direction" tabindex="-1"><a class="header-anchor" href="#whether-teleportation-changes-gravity-direction" aria-hidden="true">#</a> Whether Teleportation Changes Gravity Direction</h4><p>Whether the teleportation transforms the player&#39;s gravity location if the portal has a rotation transformation. It requires Gravity Changer (Gravity API) mod to work.</p><p>NBT tag: <code>teleportChangesGravity</code></p><h4 id="whether-to-adjust-the-entity-s-position-after-teleporting" tabindex="-1"><a class="header-anchor" href="#whether-to-adjust-the-entity-s-position-after-teleporting" aria-hidden="true">#</a> Whether to Adjust The Entity&#39;s Position After Teleporting</h4><p>If true, the player will be moved up if the player is inside the ground after teleportation.</p><p>NBT tag: <code>adjustPositionAfterTeleport</code></p><h4 id="whether-to-have-cross-portal-collision" tabindex="-1"><a class="header-anchor" href="#whether-to-have-cross-portal-collision" aria-hidden="true">#</a> Whether to Have Cross Portal Collision</h4><p>If an entity touches a portal, its collision will be specially handled, which includes:</p><ul><li>The outer collision. Things behind the portal cannot collide with that entity.</li><li>The inner collision. Things &quot;inside&quot; the portal can collide with that entity.</li></ul><p>This attribute defines whether the inner collision is being handled.</p><p>NBT tag: <code>hasCrossPortalCollision</code></p><h4 id="commands-to-invoke-after-teleporting" tabindex="-1"><a class="header-anchor" href="#commands-to-invoke-after-teleporting" aria-hidden="true">#</a> Commands to Invoke After Teleporting</h4><p>Optional. You can specify a list of commands to be invoked for entities that teleport through this portal. The command sender will be the entity that teleported.</p><p>The commands will be invoked with level-2 permission. Using command <code>/portal set_portal_nbt</code> command to change this requires level-2 permission.</p><p>NBT tag: <code>commandsOnTeleported</code></p><p>Example: Make the portal to damage the entities that cross this portal <code>/portal set_portal_nbt {commandsOnTeleported:[&quot;/effect give @s minecraft:instant_damage 1&quot;]}</code></p><p>If the command contains quotation mark <code>&quot;</code>, you need to change it to <code>\\&quot;</code>. Example: <code>/portal set_portal_nbt {commandsOnTeleported:[&quot;/say \\&quot;hi\\&quot;&quot;]}</code></p><h3 id="accessibility-attributes" tabindex="-1"><a class="header-anchor" href="#accessibility-attributes" aria-hidden="true">#</a> Accessibility Attributes</h3><h4 id="teleportable" tabindex="-1"><a class="header-anchor" href="#teleportable" aria-hidden="true">#</a> Teleportable</h4><p>If set to false, you cannot teleport through but can still see through.</p><p>NBT tag: <code>teleportable</code></p><h4 id="interactable" tabindex="-1"><a class="header-anchor" href="#interactable" aria-hidden="true">#</a> Interactable</h4><p>Whether the player can place or break blocks through the portal.</p><p>NBT tag: <code>interactable</code></p><h4 id="visibility" tabindex="-1"><a class="header-anchor" href="#visibility" aria-hidden="true">#</a> Visibility</h4><p>Whether the portal is visible. An invisible portal can still do teleportation, cross-portal collision and chunk loading.</p><p>This is different to the vanilla entity visibility (the armor stand&#39;s <code>Invisible</code> tag). In the portal gun mod, when a portal is inactive, it&#39;s invisible but its vanilla visibility is still on, the overlay still renders.</p><p>NBT tag: <code>isVisible</code></p><h4 id="specific-accessor" tabindex="-1"><a class="header-anchor" href="#specific-accessor" aria-hidden="true">#</a> Specific Accessor</h4><p>Optional. The UUID of the specific player that can access this portal. If it&#39;s present but the value is null, the portal cannot be accessed by any player but can be accessed by non-player entities.</p><p>NBT tag: <code>specificPlayerId</code></p><h3 id="default-portal-animation" tabindex="-1"><a class="header-anchor" href="#default-portal-animation" aria-hidden="true">#</a> Default Portal Animation</h3><p>All portals by default will change smoothly when you use <code>/portal</code> commands to change the portal&#39;s attributes (except for the global portals).</p><p>Following things can be animated:</p><ul><li>Portal position, destination</li><li>Portal orientation</li><li>Portal rotation transformation</li><li>Portal scale transformation</li><li>Portal width and height (If the portal has a special shape, then width and height cannot be animated)</li></ul><p>NBT tag: <code>animation</code></p><blockquote><p>Start from 2.3.0, the tag becomes <code>defaultAnimation</code>.</p></blockquote><p>The <code>animation</code> tag has two sub-tags: <code>curve</code> and <code>durationTicks</code>. <code>curve</code> can be <code>linear</code>, <code>sine</code> or <code>circle</code>. The <code>curve</code> does not change trajectory, it only determine the speed change during animation.</p><p>Example:</p><ul><li><p>Disable portal animation: <code>/portal set_portal_nbt {animation:{durationTicks:0}}</code></p></li><li><p>Give the portal a linear animation with duration of 2 seconds: <code>/portal set_portal_nbt {animation:{curve:&quot;linear&quot;,durationTicks:40}}</code></p></li></ul><p>The portal animation is client-side. On the server side the portal moves abruptly. The animation can be smooth when the networking condition is not good.</p><p>The portal animation can be triggered by <code>/portal</code> commands. Using <code>/tp</code> or <code>/data</code> command won&#39;t trigger the animation.</p><p>The relative teleportation/collision is not yet implemented. If a portal moves through you, you won&#39;t get teleported.</p><h3 id="bind-cluster" tabindex="-1"><a class="header-anchor" href="#bind-cluster" aria-hidden="true">#</a> Bind-Cluster</h3><p>This mod&#39;s every portal entity is one-way and one-faced. A bi-way bi-faced nether portal consists of 4 portal entities. That 4 portal entities are called a portal cluster. To edit that bi-way bi-faced portal, 4 portal entities needs to be changed.</p><p>By enabling <code>bindCluster</code>, if you edit one portal entity using <code>/portal</code> commands, the other 3 portals will also be updated accordingly. This makes portal manipulation easier.</p><p>NBT tag: <code>bindCluster</code></p><h3 id="rendering-attributes" tabindex="-1"><a class="header-anchor" href="#rendering-attributes" aria-hidden="true">#</a> Rendering Attributes</h3><h4 id="fuse-view" tabindex="-1"><a class="header-anchor" href="#fuse-view" aria-hidden="true">#</a> Fuse View</h4><p>If true, the portal rendering will not render the sky background and maintain the depth value in the portal view area. This results in the things inside the portal and the things outside the portal being fused together.</p><p><img src="https://i.loli.net/2021/11/20/jhZId1mWuVrExe7.png" alt=""></p><p>NBT tag: <code>fuseView</code></p><p>Example: Make the portal fuse-view <code>/portal set_portal_nbt {fuseView:true}</code></p><h4 id="rendering-mergable" tabindex="-1"><a class="header-anchor" href="#rendering-mergable" aria-hidden="true">#</a> Rendering Mergable</h4><p>If true, when the portal touches another portal that has the same spacial transformation, these portals will be &quot;grouped&quot; and the rendering performance will be improved which result in higher FPS. But the front clipping functionality does not work normally when rendering the grouped portal.</p><p>NBT tag: <code>renderingMergable</code></p><h4 id="whether-to-render-yourself" tabindex="-1"><a class="header-anchor" href="#whether-to-render-yourself" aria-hidden="true">#</a> Whether to Render Yourself</h4><p>If false, you cannot see yourself (myself) from the portal.</p><p>NBT tag: <code>doRenderPlayer</code></p><h3 id="additional-attributes" tabindex="-1"><a class="header-anchor" href="#additional-attributes" aria-hidden="true">#</a> Additional Attributes</h3><h4 id="motion-affinity" tabindex="-1"><a class="header-anchor" href="#motion-affinity" aria-hidden="true">#</a> Motion Affinity</h4><p>If it&#39;s positive, then players colliding with the portal will be accelerated in the portal&#39;s facing direction. If it&#39;s negative, the player will be decelerated when moving fast.</p><p>NBT tag: <code>motionAffinity</code></p><h2 id="breakable-portal-attributes" tabindex="-1"><a class="header-anchor" href="#breakable-portal-attributes" aria-hidden="true">#</a> Breakable Portal Attributes</h2><p>Only exists for <a href="./Portals#breakable-portals">breakable portals</a>.</p><h4 id="unbreakable" tabindex="-1"><a class="header-anchor" href="#unbreakable" aria-hidden="true">#</a> Unbreakable</h4><p>If true, the portal won&#39;t break if the frame structure is broken.</p><p>NBT tag: <code>unbreakable</code></p><p>Example: Make a nether portal unbreakable <code>/portal set_portal_nbt {unbreakable:true}</code></p><h3 id="overlay-attributes" tabindex="-1"><a class="header-anchor" href="#overlay-attributes" aria-hidden="true">#</a> Overlay Attributes</h3><h4 id="overlay-block-state" tabindex="-1"><a class="header-anchor" href="#overlay-block-state" aria-hidden="true">#</a> Overlay Block State</h4><p>Optional. It&#39;s the portal overlay block&#39;s block state.</p><p>NBT tag: <code>overlayBlockState</code></p><h4 id="overlay-opacity" tabindex="-1"><a class="header-anchor" href="#overlay-opacity" aria-hidden="true">#</a> Overlay Opacity</h4><p>Overlay&#39;s opacity. Between 0 and 1.</p><p>NBT tag: <code>overlayOpacity</code></p><h4 id="overlay-offset" tabindex="-1"><a class="header-anchor" href="#overlay-offset" aria-hidden="true">#</a> Overlay Offset</h4><p>The overlay offset along the portal&#39;s facing direction.</p><p>NBT tag: <code>overlayOffset</code></p><p>Example: will give it a diamond block overlay with opacity 0.3 and offset -0.4 <code>/portal set_portal_nbt {overlayBlockState:{Name:&quot;minecraft:diamond_block&quot;}, overlayOpacity:0.3, overlayOffset:-0.4}</code></p>',111),n=[r];function d(s,h){return t(),a("div",null,n)}var p=e(i,[["render",d],["__file","Portal-Attributes.html.vue"]]);export{p as default};
